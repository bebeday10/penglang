# Why is there syntax highlighted in the output?

Simply because of how the penguin docs are built off: **Zensical**.  
To make code annotations, penguin need to ues a complicated thing called: *Making comments*.  

## The problem

`text` does not have comments.  
How will the penguins make comments in an uncommentable language?

??? question "Why doesn't `text` have comments?"
    Because it is simply text. Text is not a programming language. It is *plain text*.  
    Penguins often get this confused.

## The answer
You don't.

You use a language with comments.  
Such as: *Python*.  

!!! info inline end
    Your output is not running code.
    Do not think you can run code using saying.

Now Python, is a programming language. Therefore, it will have syntax. This is why the `is` is slightly darker than the rest of the output.  

